import React, { memo } from "react";

const Hamburger = () => {
  return (
    <div class="navbar__hamburguer jsHamburguer">
      <div class="line"></div>
    </div>
  );
};

export default memo(Hamburger);
